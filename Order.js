function Order(
    productName,
    productPrice,
    category,
    linkForPic,
  ) {
    
    this.productName = productName;
    this.productPrice = productPrice;
    this.category = category;
    this.linkForPic = linkForPic;
  
  }
  
  console.log("Order is loaded!");
